# RADIOROC2 — כלי צד מחשב

## התקנה

```bash
pip install pyserial openpyxl
```

`pyserial` נדרש לחיבור ללוח. `openpyxl` נדרש לייצוא Excel — בלעדיו הייצוא נופל חזרה ל־CSV, והשאר עובד רגיל.

## הקבצים

| קובץ | תפקיד |
|------|-------|
| `rr2_gui.py` | האפליקציה הראשית — הפעל את זה |
| `rr2_decode.py` | פענוח פרוטוקול המסגרות. **חייב לשבת באותה תיקייה** |

`rr2_gui.py` מייבא את המפענח מ־`rr2_decode.py` כדי שיהיה מקור אמת אחד לפרוטוקול. אם תשנה את המסגור בקושחה, יש לעדכן רק קובץ אחד.

## הפעלה

```bash
python rr2_gui.py            # רגיל
python rr2_gui.py --sim      # מצב סימולציה, בלי חומרה
```

**מצב הסימולציה** מייצר פסגה גאוסיאנית סביב ADC 2400 עם זנב דמוי־קומפטון, בקצב ~180 cps. שימושי כדי ללמוד את הממשק ולבדוק את זרימת העבודה לפני שהלוח מולחם.

## מבנה הדפים

```
Main ─┬─ Settings   פרמטרי ASIC + קונסולת פקודות
      ├─ Measure    מדידה עם היסטוגרמה חיה
      ├─ History    שש מדידות אחרונות + ייצוא Excel
      └─ Help       הוראות
```

## סדר עבודה מומלץ

1. **Main** → בחר פורט → Connect (או Simulator)
2. **Settings** → Channel `all` → Disable → בחר את הערוץ המחובר → Enable
3. **Settings** → **Preset CsI** ← חשוב, ראה למטה
4. **Settings** → כוונן ספים → Apply global
5. **Measure** → בחר ערוץ ו־gain → Start
6. **History** → בחר מדידה → Export to Excel

## ⚠️ למה Preset CsI חשוב

ברירת המחדל של ה־ASIC היא צעדי עיצוב של **20 ns**. זמן הדעיכה של CsI(Tl) הוא בערך **1 µs** — פי חמישים. בלי `slowShaping` המטען לא נאסף במלואו ורזולוציית האנרגיה נהרסת.

ה־preset מפעיל slow shaping בשני המסלולים, קובע peaking סביב 1.8 µs, ומותח את ה־hold delay בהתאם. זו **נקודת מוצא** — אחרי שיהיה מקור, סרוק את ה־Hold delay ותעקוב אחרי מיקום הפוטו־פיק.

## פלט Excel

חוברת עם שני גיליונות:
- **Spectrum** — `bin`, `adc_low`, `adc_high`, `adc_centre`, `counts` + גרף קו מוטמע
- **Metadata** — זמן התחלה, משך, ערוץ, gain, סה"כ ספירות, גלישות, טמפרטורה, קצב

## אחסון

מדידות נשמרות כ־JSON בתיקיית `measurements/` ליד הסקריפט. רק **שש האחרונות** נשמרות — הישנות נמחקות אוטומטית.

## פתרון בעיות

| תסמין | סיבה סבירה |
|-------|-------------|
| הפורט לא מופיע | הלוח לא מחובר, או ש־CDC לא הוגדר ב־CubeMX |
| מחובר אבל אין אירועים | הערוץ מושבת, AMUX כבוי, או סף גבוה מדי |
| קצב עצום | הסף בתוך הרעש — העלה את DAC1 |
| פסגה בתא העליון | ה־ADC רווי — עבור ל־LG או הורד hgGain |
| `dropped` עולה בסטטוס | הקישור לא עומד בקצב — צמצם ערוצים פעילים |
